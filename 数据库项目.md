# MouseToxDB — 小鼠毒理转录组数据库 项目实施文档

> **版本**：v1.0
> **最后更新**：2026-03-08
> **UI 参考**：CTD（Comparative Toxicogenomics Database）风格

---

## 1. 项目概述

### 1.1 项目定位
MouseToxDB 是一个**公开访问、只读型**的科研数据库检索与展示网站。系统整合了小鼠（Mus musculus）暴露于多种化学毒物后的转录组 DESeq2 差异分析数据，面向公开用户提供检索、浏览和下载服务。

当前阶段**不需要登录、不需要权限管理、不需要后台管理功能**。

### 1.2 核心功能
1. 全局搜索：支持按化学物名称、CAS 号、InChIKey、PubChem CID、DESEQ_ID 检索
2. 分析条目浏览：以表格形式浏览所有分析条目，支持侧边栏筛选
3. 详情页展示：展示分析条目的完整信息、样本记录、PDF 图表资源、DEG 表格
4. 数据下载：支持下载整库 Excel、当前搜索结果导出
5. 外部链接跳转：PubChem、GEO、SRA、BioProject、DOI
6. Docker 一键启动

### 1.3 UI 语言
- v1 界面使用**中文**
- 代码结构上预留国际化（i18n）能力，方便后续增加中英文切换

---

## 2. 技术栈

### 2.1 前端
| 技术 | 用途 |
|------|------|
| React 18 | UI 框架 |
| TypeScript | 类型安全 |
| Vite | 构建工具 |
| Ant Design | 表格、分页、表单、布局等 UI 组件 |
| React Router v6 | 页面路由 |
| react-pdf 或 PDF.js | 前端内嵌 PDF 预览 |
| Axios | HTTP 请求 |

### 2.2 后端
| 技术 | 用途 |
|------|------|
| FastAPI | Web 框架（自带 `/docs` Swagger 文档） |
| SQLAlchemy 2.0 | ORM |
| Pydantic v2 | 请求/响应数据校验 |
| openpyxl / pandas | Excel 导入脚本 |
| Python 3.11+ | 运行时 |

> **说明**：不使用 Alembic。本项目为只读型数据库，表结构由导入脚本创建，无需迁移管理。

### 2.3 数据库
| 配置项 | 值 |
|--------|-----|
| 引擎 | MySQL 8.0 |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_general_ci（大小写不敏感） |
| 数据库名 | mousetoxdb |

### 2.4 文件与静态资源
- 资源文件（PDF/TXT）存储在**服务器本地文件系统**
- 按 `DESEQ_ID` 组织目录：`/data/deseq_assets/{DESEQ_ID}/`
- 数据库只保存资源路径与元数据，不保存二进制文件

### 2.5 部署
| 服务 | 说明 |
|------|------|
| `nginx` | 反向代理 + 前端静态文件 + 静态资源服务 |
| `backend` | FastAPI 应用 |
| `mysql` | 数据库 |

> 前端通过 Nginx 多阶段构建打包为静态文件，无需独立前端容器。共 **3 个 Docker 服务**。

---

## 3. 数据源分析

### 3.1 Excel 数据
- 文件：`260307小鼠双端信息全.xlsx`
- 工作表：`Sheet1`
- 表头行：**第 3 行**（第 17 列表头为空格，已确认命名为 `LibraryLayout`）
- 数据起始行：**第 4 行**
- 数据总行数：**986**
- 源字段数：**43**

### 3.2 数据规模统计
| 指标 | 数量 |
|------|------|
| 原始样本记录数 | 986 |
| 唯一化学物数（按 `ID` 列） | 36 |
| 唯一 `DESEQ_ID` 数 | 78 |
| 唯一 `SRR_ID` 数 | 770 |
| 唯一 `GSE_ID` 数 | 40 |
| 唯一 `BioProject` 数 | 40 |
| 去重后分析条目数（按 `analysis_key`） | 81 |

### 3.3 DESEQ_ID 与 analysis_key 的关系

Excel 中每行代表一个 SRR 样本。同一个 `DESEQ_ID` 下通常包含多个样本（例如 DESEQ0043 有 43 个样本）——这是**正常行为**。

**但有 3 个 DESEQ_ID 被复用于不同的分析条目**（即同一编号对应不同的化学物/GSE 组合）：

| DESEQ_ID | 对应的 analysis_key | 说明 |
|----------|-------------------|------|
| DESEQ0066 | `DESEQ0066__1515__GSE182897__PRJNA758256` | 两个不同化学物/GSE |
| | `DESEQ0066__296__GSE162527__PRJNA682163` | |
| DESEQ0078 | `DESEQ0078__300__GSE233282__PRJNA975566` | 两个不同化学物/GSE |
| | `DESEQ0078__13__GSE235586__PRJNA986506` | |
| DESEQ1011 | `DESEQ1011__585__GSE136174__PRJNA561500` | 同一 GSE，不同化学物 |
| | `DESEQ1011__567__GSE136174__PRJNA561500` | |

因此：
- `DESEQ_ID` **不能作为唯一键**
- 需要 `analysis_key` 作为系统内部唯一标识
- 资源文件仍按 `DESEQ_ID` 关联（共享同一份资源不影响正确性）
- 前端展示 `DESEQ_ID`，URL 路由使用 `analysis_key`

### 3.4 资源文件现状

当前 demo 目录包含 **35 个 DESEQ 文件夹**（含 1 个 zip 包）。

| 情况 | 数量 | 说明 |
|------|------|------|
| 与 Excel 匹配且有资源文件 | 33 | 正常 |
| 仅在 Demo 中，Excel 无对应记录 | 1 | `DESEQ0005`（资源已存在，主表暂未匹配） |
| 文件夹为空（0 个文件） | 2 | `DESEQ0066`、`DESEQ1016` |
| 仅在 Excel 中，无资源文件夹 | 44 | 资源待补充 |

资源完整性情况：
| 文件数 | 文件夹数 | 说明 |
|--------|---------|------|
| 11（完整） | 26 | 含所有 9 个 PDF + 2 个 TXT |
| 10 | 4 | 缺少 `DO_Up_plot.pdf` |
| 9 | 3 | 缺少 `DO_Up_plot.pdf` + `Heatmap.pdf` |
| 0 | 2 | 空文件夹 |

---

## 4. 数据库设计

### 4.1 主表：`main_records`

保存 Excel 中的所有原始字段，每一行对应 Excel 的一条样本记录。

#### 系统内部字段（非 Excel 源字段）
| 字段 | 类型 | 说明 |
|------|------|------|
| `record_pk` | BIGINT AUTO_INCREMENT PRIMARY KEY | 数据库主键 |
| `analysis_key` | VARCHAR(128) NOT NULL | 系统内部分析键（同一值对应多行 SRR 样本，非 UNIQUE） |
| `created_at` | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 建表原则
1. Excel 的 43 个源字段全部保留
2. 数据库字段统一使用英文 snake_case
3. `record_pk` 作为数据库主键
4. `analysis_key` 作为系统内部唯一分析键（生成规则：`{DESEQ_ID}__{ID}__{GSE_ID}__{BioProject}`）
5. `DESEQ_ID` 作为检索字段和资源关联字段，**不设唯一约束**

#### 完整建表 SQL

```sql
CREATE TABLE main_records (
    -- 系统字段
    record_pk          BIGINT AUTO_INCREMENT PRIMARY KEY,
    analysis_key       VARCHAR(128) NOT NULL,
    created_at         DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at         DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    -- Excel 源字段（43 列）
    sort_id                  INT          NOT NULL                COMMENT 'Excel原始排序号',
    chemical_id              INT          NOT NULL                COMMENT '化学物质内部编号(ID列)',
    cas_id                   VARCHAR(50)  NOT NULL                COMMENT 'CAS编号',
    inchi_key                VARCHAR(100) NOT NULL                COMMENT 'InChIKey',
    chemical_name            VARCHAR(512) NOT NULL                COMMENT '化学物标准名称',
    alternative_names        TEXT         NULL                    COMMENT '化学物别名',
    pubchem_cid              VARCHAR(50)  NOT NULL                COMMENT 'PubChem化合物编号',
    pubchem_name             VARCHAR(255) NOT NULL                COMMENT 'PubChem名称',
    deseq_id                 VARCHAR(50)  NOT NULL                COMMENT 'DESeq分析编号',
    from_group               VARCHAR(255) NOT NULL                COMMENT '数据来源分组',
    evidence                 VARCHAR(255) NOT NULL                COMMENT '证据等级说明',
    gse_id                   VARCHAR(50)  NOT NULL                COMMENT 'GEO Series编号',
    srr_id                   VARCHAR(50)  NOT NULL                COMMENT 'SRA Run编号',
    bioproject_id            VARCHAR(50)  NOT NULL                COMMENT 'BioProject编号',
    avg_spot_len             INT          NOT NULL                COMMENT '平均reads/spot长度',
    cell_type                VARCHAR(255) NULL                    COMMENT '细胞类型/样本来源',
    library_layout           VARCHAR(20)  NOT NULL                COMMENT '测序布局(PAIRED/SINGLE)',
    organism                 VARCHAR(100) NOT NULL                COMMENT '物种',
    platform                 VARCHAR(50)  NOT NULL                COMMENT '测序平台',
    treatment                VARCHAR(255) NULL                    COMMENT '处理条件',
    experiment_group         VARCHAR(20)  NOT NULL                COMMENT '实验分组(con/treat)',
    chem_name                VARCHAR(255) NULL                    COMMENT '实验中化学名称/简称',
    dose                     VARCHAR(100) NULL                    COMMENT '暴露剂量',
    exposure_time            VARCHAR(100) NULL                    COMMENT '暴露时间',
    tissue_category          VARCHAR(100) NOT NULL                COMMENT '组织一级分类',
    tissue_subcategory       VARCHAR(100) NULL                    COMMENT '组织二级分类',
    reproductive_subcategory VARCHAR(100) NULL                    COMMENT '生殖系统细分',
    tissue_or_cell_line      VARCHAR(255) NOT NULL                COMMENT '具体组织或细胞系',
    exposure_toxicant        VARCHAR(255) NULL                    COMMENT '暴露毒物名称',
    library_method           VARCHAR(100) NULL                    COMMENT '建库方法',
    library_method_detail    VARCHAR(100) NULL                    COMMENT '详细建库方法',
    publication_year         SMALLINT     NULL                    COMMENT '发表年份',
    publication_month        TINYINT      NULL                    COMMENT '发表月份',
    reference_title          TEXT         NULL                    COMMENT '参考文献标题',
    doi                      VARCHAR(255) NULL                    COMMENT 'DOI编号',
    class1_code              VARCHAR(20)  NOT NULL                COMMENT 'MESH分类层级1',
    class2_code              VARCHAR(20)  NOT NULL                COMMENT 'MESH分类层级2',
    class3_name              VARCHAR(100) NULL                    COMMENT '分类层级3',
    class4_name              VARCHAR(100) NULL                    COMMENT '分类层级4',
    class5_name              VARCHAR(100) NULL                    COMMENT '分类层级5',
    class6_name              VARCHAR(100) NULL                    COMMENT '分类层级6',
    class7_name              VARCHAR(100) NULL                    COMMENT '分类层级7',
    inferred_class           VARCHAR(100) NULL                    COMMENT '推断分类',

    -- 索引
    INDEX idx_analysis_key (analysis_key),  -- 非 UNIQUE：同一 analysis_key 对应多行 SRR 样本
    INDEX idx_chemical_id (chemical_id),
    INDEX idx_cas_id (cas_id),
    INDEX idx_inchi_key (inchi_key),
    INDEX idx_pubchem_cid (pubchem_cid),
    INDEX idx_deseq_id (deseq_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
  COMMENT='主表：Excel样本记录';
```

### 4.2 资源表：`record_assets`

保存与 `DESEQ_ID` 关联的 PDF/TXT 文件元信息。

```sql
CREATE TABLE record_assets (
    asset_id       BIGINT AUTO_INCREMENT PRIMARY KEY,
    deseq_id       VARCHAR(50)  NOT NULL                     COMMENT '资源所属DESEQ_ID',
    file_name      VARCHAR(255) NOT NULL                     COMMENT '原始文件名',
    file_path      TEXT         NOT NULL                     COMMENT '服务器相对路径',
    file_ext       VARCHAR(10)  NOT NULL                     COMMENT '文件扩展名(pdf/txt)',
    asset_category VARCHAR(50)  NOT NULL                     COMMENT '资源分类',
    display_name   VARCHAR(100) NOT NULL                     COMMENT '前端显示名称',
    sort_order     INT          NOT NULL DEFAULT 0           COMMENT '展示排序',
    parse_mode     VARCHAR(20)  NOT NULL DEFAULT 'none'      COMMENT '解析模式(none/table_preview)',
    status         VARCHAR(20)  NOT NULL DEFAULT 'available' COMMENT '状态(available/missing/pending)',
    remark         TEXT         NULL                         COMMENT '备注',
    created_at     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_deseq_id (deseq_id),
    INDEX idx_asset_category (asset_category),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
  COMMENT='资源表：DESEQ_ID关联的PDF/TXT文件';
```

#### status 字段取值
| 值 | 含义 |
|------|------|
| `available` | 文件存在且可访问 |
| `missing` | 文件预期存在但实际缺失 |
| `pending` | 资源待补充（文件夹为空或尚未上传） |

---

## 5. 数据导入与清洗规则

### 5.1 Excel 导入规则
1. 只读取 `Sheet1`
2. 第 3 行作为字段名
3. 第 4 行开始为数据
4. 导入时统一执行：
   - 去除首尾空格（含 `\xa0` 等不可见空白字符）
   - 空字符串转为 `NULL`
   - 第 17 列（表头为空格）映射为 `library_layout`

### 5.2 "0" 作为缺失值的字段
下列字段中，若值为裸字符 `"0"` 或数值 `0`，统一按 `NULL` 处理：

`dose`、`tissue_subcategory`、`reproductive_subcategory`、`exposure_toxicant`、`library_method`、`library_method_detail`、`publication_year`、`publication_month`、`reference_title`、`doi`、`class3_name`、`class4_name`、`class5_name`、`class6_name`、`class7_name`、`inferred_class`

> 例外：`"0 ppm"`、`"0.1 mg/kg"` 这类带单位的字符串不视为缺失。

### 5.3 数值字段规范化
以下字段导入时转为整数：
- `sort_id`（对应 `Sort_ID`）
- `chemical_id`（对应 `ID`）
- `avg_spot_len`（对应 `AvgSpotLen`）
- `publication_year`（对应 `发表年（1900-2030）`）
- `publication_month`（对应 `发表月（1-12）`）

### 5.4 生成 `analysis_key`
```
analysis_key = f"{deseq_id}__{chemical_id}__{gse_id}__{bioproject_id}"
```

- 前端展示仍用 `DESEQ_ID`
- URL 路由使用 `analysis_key`
- 避免 `DESEQ_ID` 复用导致详情页错乱

### 5.5 Excel 列名 → 数据库字段名映射

| Excel 列名 | 数据库字段名 |
|-----------|-----------|
| Sort_ID | sort_id |
| ID | chemical_id |
| CAS_ID | cas_id |
| InChIKey | inchi_key |
| Chemical_Name | chemical_name |
| Alternative_Names | alternative_names |
| PubChem_CID | pubchem_cid |
| PubChem_Name | pubchem_name |
| DESEQ_ID | deseq_id |
| FromGroup | from_group |
| Evidence | evidence |
| GSE_ID | gse_id |
| SRR_ID | srr_id |
| BioProject | bioproject_id |
| AvgSpotLen | avg_spot_len |
| cell_type | cell_type |
| *(第17列空白)* | library_layout |
| Organism | organism |
| Platform | platform |
| treatment | treatment |
| Group | experiment_group |
| Chem_Name | chem_name |
| 剂量 | dose |
| 时间 | exposure_time |
| 组织分类 | tissue_category |
| 组织细分 | tissue_subcategory |
| 生殖细分 | reproductive_subcategory |
| 组织/细胞系 | tissue_or_cell_line |
| 暴露毒物 | exposure_toxicant |
| 建库方法 | library_method |
| 详细建库方法 | library_method_detail |
| 发表年（1900-2030） | publication_year |
| 发表月（1-12） | publication_month |
| 参考文献 | reference_title |
| DOI | doi |
| Class1 | class1_code |
| Class2 | class2_code |
| Class3 | class3_name |
| Class4 | class4_name |
| Class5 | class5_name |
| Class6 | class6_name |
| Class7 | class7_name |
| infer_Class | inferred_class |

---

## 6. 资源文件存储与解析

### 6.1 目录结构
```text
/data/deseq_assets/
├── DESEQ0005/
│   ├── DESEQ0005_PCA_plot.pdf
│   ├── DESEQ0005_Volcano_plot.pdf
│   ├── DESEQ0005_Heatmap.pdf
│   ├── DESEQ0005_KEGG_Up_plot.pdf
│   ├── DESEQ0005_KEGG_Down_plot.pdf
│   ├── DESEQ0005_Reactome_Up_plot.pdf
│   ├── DESEQ0005_Reactome_Down_plot.pdf
│   ├── DESEQ0005_Hallmark_Up_plot.pdf
│   ├── DESEQ0005_DO_Up_plot.pdf
│   ├── DESEQ0005_deg_table.txt
│   └── DESEQ0005_plot_df.txt
├── DESEQ0026/
│   └── ...
```

### 6.2 资源命名映射规则

资源扫描脚本按文件名关键词自动识别类别：

| 文件名关键词 | asset_category | 显示名称 | sort_order | parse_mode |
|------------|---------------|---------|-----------|------------|
| `PCA_plot` | pca_plot | PCA 图 | 10 | none |
| `Volcano_plot` | volcano_plot | 火山图 | 20 | none |
| `Heatmap` | heatmap | 热图 | 30 | none |
| `KEGG_Up_plot` | kegg_up_plot | KEGG 上调通路 | 40 | none |
| `KEGG_Down_plot` | kegg_down_plot | KEGG 下调通路 | 50 | none |
| `Reactome_Up_plot` | reactome_up_plot | Reactome 上调通路 | 60 | none |
| `Reactome_Down_plot` | reactome_down_plot | Reactome 下调通路 | 70 | none |
| `Hallmark_Up_plot` | hallmark_up_plot | Hallmark 上调通路 | 80 | none |
| `DO_Up_plot` | do_up_plot | DO 上调通路 | 90 | none |
| `deg_table.txt` | deg_table | DEG 差异基因表 | 100 | table_preview |
| `plot_df.txt` | plot_df | 绘图数据 | 110 | none |

### 6.3 资源状态处理

| 情况 | status | 前端展示 |
|------|--------|---------|
| 文件存在且可读取 | available | 正常展示/下载 |
| 资源表有记录但文件缺失 | missing | 显示"当前文件未找到" |
| 文件夹为空或尚未上传 | pending | 显示"资源待补充" |
| 文件夹不存在 | — | 详情页资源区显示"暂无资源" |

### 6.4 TXT 文件解析

#### `deg_table.txt`
- 格式：Tab 分隔（TSV）
- 列：`geneID`, `baseMean`, `log2FoldChange`, `lfcSE`, `stat`, `pvalue`, `padj`, `-log10(P.Value)`, `trend`, `ENTREZID`
- 前端以分页表格展示，每页 30 行
- 支持全量浏览

#### `plot_df.txt`
- 格式：Tab 分隔（TSV），与 `deg_table.txt` 结构相同
- v1 仅支持**下载**，不做表格预览
- 后续版本可增加预览功能

---

## 7. 搜索与结果展示

### 7.1 搜索入口
- 首页和结果页均保留**全局搜索框**
- 搜索框左侧提供**可选的分类下拉菜单**，支持缩小搜索范围：

| 分类选项 | 搜索字段 | 匹配方式 |
|---------|---------|---------|
| 全部（默认） | 下方 7 个字段同时匹配 | 混合 |
| CAS 号 | cas_id | 精确匹配 |
| InChIKey | inchi_key | 精确匹配 |
| 化学物名称 | chemical_name, alternative_names, pubchem_name | 模糊匹配 |
| PubChem CID | pubchem_cid | 精确匹配 |
| DESEQ_ID | deseq_id | 精确匹配 |

### 7.2 全局搜索字段（"全部"模式）
| 字段 | 匹配方式 |
|------|---------|
| cas_id | 精确匹配（大小写不敏感） |
| inchi_key | 精确匹配（大小写不敏感） |
| pubchem_cid | 精确匹配（大小写不敏感） |
| deseq_id | 精确匹配（大小写不敏感） |
| chemical_name | 模糊匹配（`LIKE %keyword%`） |
| alternative_names | 模糊匹配（`LIKE %keyword%`） |
| pubchem_name | 模糊匹配（`LIKE %keyword%`） |

> 所有搜索均大小写不敏感（由 `utf8mb4_general_ci` 排序规则保证）。

### 7.3 搜索结果层级
搜索结果返回**分析条目级**（按 `analysis_key` 去重），而非样本级：
- 每个结果项代表一个分析条目
- 显示字段：`DESEQ_ID`、`Chemical_Name`、`CAS_ID`、`GSE_ID`、`Organism`、`组织分类`、`建库方法`、`样本数`
- 点击 `DESEQ_ID` 进入详情页

### 7.4 排序与分页
- 默认排序：先按 `chemical_id` 升序，再按 `deseq_id` 升序
- 每页 **30** 条
- 超过 30 条启用分页

### 7.5 结果导出
- 搜索结果页上方提供**"导出结果"按钮**
- 导出当前搜索命中的所有分析条目为 Excel 文件
- 文件名：`MouseToxDB_搜索结果_YYYYMMDD_HHMMSS.xlsx`

### 7.6 空结果处理
- 0 条结果：显示"未找到相关数据，请尝试其他关键词"
- 1 条及以上：列表 + 分页

---

## 8. 前端页面结构

### 8.1 导航栏
参考 CTD 风格的顶部导航栏：

| 导航项 | 路由 | 说明 |
|--------|------|------|
| 首页 | `/` | 搜索 + 数据摘要 + 统计概览 |
| 浏览 | `/browse` | 表格浏览所有分析条目 |
| 下载 | `/download` | 整库下载 |
| 统计 | `/statistics` | 静态统计图 |
| 帮助 | `/help` | 使用说明、字段说明、联系方式 |

右上角保留全局搜索框（精简版），搜索后跳转至 `/search?keyword=xxx`。

### 8.2 首页（`/`）

#### 布局（从上到下）

**A. Hero 区域**
- 项目名称：MouseToxDB
- 副标题：小鼠毒理转录组数据库
- 简介：一段说明项目的文字（2-3 句）

**B. 搜索区**
- 居中的搜索框 + 分类下拉
- 搜索框 placeholder：输入化学物名称、CAS号、InChIKey、PubChem CID 或 DESEQ_ID
- 搜索按钮

**C. 数据摘要卡片区**（4 张卡片，一行排列）
| 卡片 | 数值 | 说明 |
|------|------|------|
| 样本记录 | 986 | 总样本数 |
| 分析条目 | 81 | 唯一分析数 |
| 化学物种类 | 36 | 唯一化学物数 |
| DESEQ 分析数 | 78 | 唯一 DESEQ_ID 数 |

> 数值来自 `/api/stats/summary` 接口，动态获取。

**D. 统计图区**
- 静态图片展示（v1）
- 预留：组织分布图、数据来源饼图等

**E. 分析流程图区**
- 静态示意图：展示数据从 FASTQ → DESeq2 分析 → 富集分析 → MouseToxDB 的流程

**F. 外部链接区**
- GEO、PubChem、BioProject 等常用外链入口

### 8.3 搜索结果页（`/search?keyword=xxx&category=all`）

- 页面顶部保留搜索框（支持重新搜索）
- 结果提示：显示"找到 N 条分析条目"
- **"导出结果"按钮**：导出当前搜索结果为 Excel
- 结果表格列：

| 列名 | 字段 | 可点击 |
|------|------|--------|
| DESEQ_ID | deseq_id | 是（跳转详情页） |
| 化学物名称 | chemical_name | 否 |
| CAS 号 | cas_id | 否 |
| GSE_ID | gse_id | 是（跳转 GEO 外链） |
| 物种 | organism | 否 |
| 组织分类 | tissue_category | 否 |
| 建库方法 | library_method | 否 |
| 样本数 | sample_count（聚合计算） | 否 |

- 每页 30 条，支持分页

### 8.4 浏览页（`/browse`）

#### 布局
- 左侧：**筛选面板**
- 右侧：**数据表格**

#### 筛选面板
| 筛选项 | 类型 | 数据来源 |
|--------|------|---------|
| 组织分类 | 多选下拉/复选框 | 从数据库聚合 `tissue_category` 的唯一值 |
| 建库方法 | 多选下拉/复选框 | 从数据库聚合 `library_method` 的唯一值 |
| 发表年份 | 范围选择 | `publication_year` 的最小值 ~ 最大值 |

> 物种筛选：当前数据均为 Mus musculus，暂不展示物种筛选。后续增加其他物种数据时再添加。

#### 数据表格
- 与搜索结果页表格列一致
- 默认展示所有分析条目（按 `chemical_id` 升序）
- 支持分页（每页 30 条）
- 点击 `DESEQ_ID` 跳转详情页

### 8.5 下载页（`/download`）

| 下载项 | 说明 | 按钮文字 |
|--------|------|---------|
| 完整数据库 Excel | 下载原始 Excel 文件 | 下载完整数据 |

> 搜索结果导出功能在搜索结果页提供，不在下载页重复。

### 8.6 统计页（`/statistics`）
- v1 使用**静态图片**
- 页面结构：
  - 图片标题
  - 图片展示区
  - 图片说明文字
- 后续替换为动态交互式图表

### 8.7 帮助页（`/help`）
- 网站使用说明
- 字段说明简表
- 外部链接说明（各链接的用途和格式）
- 联系方式
- 论文引用说明

---

## 9. 详情页设计

### 9.1 路由与标题
- 路由：`/analysis/:analysisKey`
- 页面标题显示：`DESEQ_ID`（如 `DESEQ1001`）
- URL 参数使用 `analysis_key`（如 `DESEQ1001__90__GSE223464__PRJNA926360`）

### 9.2 页面整体结构

参考 CTD 的 Tab 式布局，详情页分为以下区域：

```
┌─────────────────────────────────────────────────┐
│  ← 返回搜索结果    DESEQ1001 - Bisphenol AF     │
├─────────────────────────────────────────────────┤
│  [基本信息] [实验信息] [样本记录] [图表资源] [DEG表格] │
├─────────────────────────────────────────────────┤
│                                                 │
│              当前 Tab 内容区                      │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 9.3 各 Tab 内容

#### Tab 1：基本信息
使用 Ant Design Descriptions 组件，展示化学物核心信息：

| 字段 | 值示例 | 可外链 |
|------|--------|--------|
| 化学物名称 | Bisphenol AF | 否 |
| CAS 号 | 1478-61-1 | 否 |
| InChIKey | ZFVMWEVVKGLCIJ-UHFFFAOYSA-N | 否 |
| PubChem CID | 7175 | 是 → PubChem |
| PubChem 名称 | Bisphenol AF | 否 |
| DESEQ_ID | DESEQ1001 | 否 |
| 数据来源 | EU_EDCs,DEDuCT,TEDX | 否 |
| 证据等级 | (1)人类和小鼠证据 | 否 |
| MESH 分类 | Class1 ~ Class7 + inferred_class | 否 |

#### Tab 2：实验信息

| 字段 | 值示例 | 可外链 |
|------|--------|--------|
| GSE_ID | GSE223464 | 是 → GEO |
| BioProject | PRJNA926360 | 是 → BioProject |
| 物种 | Mus musculus | 否 |
| 测序平台 | ILLUMINA | 否 |
| 组织分类 | 癌症 | 否 |
| 组织细分 | 生殖系统癌症 | 否 |
| 生殖细分 | — | 否 |
| 组织/细胞系 | 人乳腺癌细胞（MCF7 细胞系） | 否 |
| 暴露毒物 | BPAF | 否 |
| 建库方法 | bulk_RNAseq | 否 |
| 详细建库方法 | RNA-seq | 否 |
| 发表年份 | 2017 | 否 |
| 发表月份 | 8 | 否 |
| 参考文献 | Editor's Highlight: ... | 否 |
| DOI | 10.1093/toxsci/kfx101 | 是 → DOI |

#### Tab 3：样本记录

展示当前分析条目关联的所有 SRR 样本记录，表格列：

| 列 | 字段 | 可外链 |
|----|------|--------|
| SRR_ID | srr_id | 是 → SRA |
| 处理条件 | treatment | 否 |
| 实验分组 | experiment_group | 否 |
| 化学简称 | chem_name | 否 |
| 剂量 | dose | 否 |
| 暴露时间 | exposure_time | 否 |
| AvgSpotLen | avg_spot_len | 否 |
| 细胞类型 | cell_type | 否 |
| 测序布局 | library_layout | 否 |

#### Tab 4：图表资源

- 以**卡片网格布局**展示 PDF 资源
- 每张卡片包含：
  - 图表名称（如"PCA 图"、"火山图"）
  - PDF 内嵌预览（使用 react-pdf）
  - 下载按钮
- 资源按 `sort_order` 排序
- 缺失资源的卡片显示：灰色占位 + "当前文件未找到" 或 "资源待补充"
- `plot_df.txt` 在本 Tab 底部提供下载链接

#### Tab 5：DEG 差异基因表

- 解析 `deg_table.txt` 为表格
- 表格列：`geneID`, `baseMean`, `log2FoldChange`, `lfcSE`, `stat`, `pvalue`, `padj`, `-log10(P.Value)`, `trend`, `ENTREZID`
- 每页 30 行，支持分页
- 若解析失败，显示"文件解析失败"并保留下载按钮
- 若文件不存在，显示"暂无 DEG 数据"

### 9.4 外部链接规则

| 字段 | 跳转模板 | 说明 |
|------|---------|------|
| PubChem CID | `https://pubchem.ncbi.nlm.nih.gov/compound/{pubchem_cid}` | |
| GSE_ID | `https://www.ncbi.nlm.nih.gov/geo/query/acc.cgi?acc={gse_id}` | |
| SRR_ID | `https://www.ncbi.nlm.nih.gov/sra/?term={srr_id}` | |
| BioProject | `https://www.ncbi.nlm.nih.gov/bioproject/{bioproject_id}` | |
| DOI | `https://doi.org/{normalized_doi}` | 若原值含 `doi:` 前缀，需先去除 |

所有外链在**新标签页**打开。字段值为空时不显示链接。

---

## 10. API 设计

### 10.0 统一响应格式

所有 API 返回统一 JSON 结构：

```json
// 成功
{
  "code": 200,
  "message": "success",
  "data": { ... }
}

// 错误（如 404）
{
  "code": 404,
  "message": "分析条目未找到",
  "data": null
}

// 列表类（带分页）
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 81,
    "page": 1,
    "page_size": 30,
    "items": [ ... ]
  }
}
```

### 10.1 搜索接口
`GET /api/search`

请求参数：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| keyword | string | 是 | — | 搜索关键词 |
| category | string | 否 | all | 搜索分类：all / cas / inchikey / chemical_name / pubchem_cid / deseq_id |
| page | int | 否 | 1 | 页码 |
| page_size | int | 否 | 30 | 每页条数（最大 30） |

返回 `data` 示例：
```json
{
  "total": 1,
  "page": 1,
  "page_size": 30,
  "items": [
    {
      "analysis_key": "DESEQ1001__90__GSE223464__PRJNA926360",
      "deseq_id": "DESEQ1001",
      "chemical_id": 90,
      "sort_id": 559,
      "cas_id": "1478-61-1",
      "chemical_name": "Bisphenol AF",
      "pubchem_cid": "7175",
      "pubchem_name": "Bisphenol AF",
      "gse_id": "GSE223464",
      "bioproject_id": "PRJNA926360",
      "organism": "Mus musculus",
      "tissue_category": "癌症",
      "library_method": "bulk_RNAseq",
      "platform": "ILLUMINA",
      "publication_year": 2017,
      "sample_count": 6,
      "has_assets": true
    }
  ]
}
```

### 10.2 浏览接口
`GET /api/browse`

请求参数：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| tissue_category | string | 否 | — | 组织分类筛选（多个用逗号分隔） |
| library_method | string | 否 | — | 建库方法筛选（多个用逗号分隔） |
| year_min | int | 否 | — | 发表年份最小值 |
| year_max | int | 否 | — | 发表年份最大值 |
| page | int | 否 | 1 | 页码 |
| page_size | int | 否 | 30 | 每页条数 |

返回格式与搜索接口的 `items` 结构一致。

### 10.3 浏览筛选选项接口
`GET /api/browse/filters`

返回 `data` 示例：
```json
{
  "tissue_categories": ["癌症", "生殖", "肝脏", "..."],
  "library_methods": ["bulk_RNAseq", "..."],
  "year_range": { "min": 2015, "max": 2024 }
}
```

### 10.4 详情接口
`GET /api/analysis/{analysis_key}`

返回 `data` 示例：
```json
{
  "analysis_key": "DESEQ1001__90__GSE223464__PRJNA926360",
  "deseq_id": "DESEQ1001",
  "summary": {
    "sort_id": 559,
    "chemical_id": 90,
    "cas_id": "1478-61-1",
    "inchi_key": "ZFVMWEVVKGLCIJ-UHFFFAOYSA-N",
    "chemical_name": "Bisphenol AF",
    "alternative_names": "Bisphenol AF",
    "pubchem_cid": "7175",
    "pubchem_name": "Bisphenol AF",
    "from_group": "EU_EDCs,DEDuCT,TEDX",
    "evidence": "(1)人类和小鼠证据",
    "gse_id": "GSE223464",
    "bioproject_id": "PRJNA926360",
    "organism": "Mus musculus",
    "platform": "ILLUMINA",
    "tissue_category": "癌症",
    "tissue_subcategory": "生殖系统癌症",
    "reproductive_subcategory": null,
    "tissue_or_cell_line": "人乳腺癌细胞（MCF7 细胞系）",
    "exposure_toxicant": "BPAF",
    "library_method": "bulk_RNAseq",
    "library_method_detail": "RNA-seq",
    "publication_year": 2017,
    "publication_month": 8,
    "reference_title": "Editor's Highlight: ...",
    "doi": "10.1093/toxsci/kfx101",
    "class1_code": "D02",
    "class2_code": "D02",
    "class3_name": "碳氢化合物",
    "class4_name": "碳氢化合物，环状",
    "class5_name": "芳烃",
    "class6_name": "苯衍生物",
    "class7_name": "酚类",
    "inferred_class": "双酚类（旧）"
  },
  "sample_records": [
    {
      "srr_id": "SRR23190066",
      "avg_spot_len": 300,
      "cell_type": "uterus",
      "library_layout": "PAIRED",
      "treatment": "BPAF",
      "experiment_group": "treat",
      "chem_name": "BPAF",
      "dose": "300 μg/kg",
      "exposure_time": "28d"
    }
  ],
  "assets": [
    {
      "asset_id": 101,
      "deseq_id": "DESEQ1001",
      "display_name": "PCA 图",
      "asset_category": "pca_plot",
      "file_ext": "pdf",
      "preview_url": "/static/assets/DESEQ1001/DESEQ1001_PCA_plot.pdf",
      "download_url": "/api/assets/101/download",
      "status": "available"
    }
  ]
}
```

### 10.5 DEG 表格分页接口
`GET /api/analysis/{analysis_key}/tables/deg-table`

请求参数：
| 参数 | 类型 | 默认值 |
|------|------|--------|
| page | int | 1 |
| page_size | int | 30 |

返回 `data` 示例：
```json
{
  "name": "deg_table",
  "columns": ["geneID", "baseMean", "log2FoldChange", "lfcSE", "stat", "pvalue", "padj", "-log10(P.Value)", "trend", "ENTREZID"],
  "total": 378,
  "page": 1,
  "page_size": 30,
  "items": [
    {
      "geneID": "Krt6b",
      "baseMean": 775.4847,
      "log2FoldChange": -9.8098,
      "lfcSE": 2.2982,
      "stat": -4.2684,
      "pvalue": 1.97e-05,
      "padj": 0.00349,
      "-log10(P.Value)": 4.7058,
      "trend": "down",
      "ENTREZID": "16688"
    }
  ]
}
```

### 10.6 资源列表接口
`GET /api/analysis/{analysis_key}/assets`

返回当前分析条目对应的资源列表（通过 `deseq_id` 关联）。

### 10.7 单个资源下载接口
`GET /api/assets/{asset_id}/download`

返回文件流（`Content-Disposition: attachment`）。

### 10.8 整库下载接口
`GET /api/download/database`

返回原始 Excel 文件流。

### 10.9 搜索结果导出接口
`GET /api/search/export`

请求参数：
| 参数 | 类型 | 说明 |
|------|------|------|
| keyword | string | 搜索关键词 |
| category | string | 搜索分类 |

返回生成的 Excel 文件流。

### 10.10 统计摘要接口
`GET /api/stats/summary`

返回 `data` 示例：
```json
{
  "record_rows": 986,
  "analysis_groups": 81,
  "unique_chemicals": 36,
  "unique_deseq_id": 78,
  "unique_srr_id": 770,
  "unique_gse_id": 40,
  "unique_bioproject_id": 40,
  "statistics_assets": [
    {
      "name": "overall_summary",
      "title": "数据总览",
      "type": "image",
      "url": "/static/statistics/overall_summary.png"
    }
  ]
}
```

---

## 11. 字段数据字典

| 显示名 | 数据库字段名 | 类型 | 可空 | 索引 | 全局搜索 | 示例 |
|--------|-----------|------|------|------|---------|------|
| Sort_ID | sort_id | INT | 否 | — | 不参与；原始排序保留 | 559 |
| ID | chemical_id | INT | 否 | BTREE | 不参与；结果排序字段 | 90 |
| CAS_ID | cas_id | VARCHAR(50) | 否 | BTREE | 精确匹配 | 1478-61-1 |
| InChIKey | inchi_key | VARCHAR(100) | 否 | BTREE | 精确匹配 | ZFVMWEVVKGLCIJ-UHFFFAOYSA-N |
| Chemical_Name | chemical_name | VARCHAR(512) | 否 | — | 模糊匹配 | Bisphenol AF |
| Alternative_Names | alternative_names | TEXT | 是 | — | 模糊匹配 | Bisphenol AF |
| PubChem_CID | pubchem_cid | VARCHAR(50) | 否 | BTREE | 精确匹配 | 7175 |
| PubChem_Name | pubchem_name | VARCHAR(255) | 否 | — | 模糊匹配 | Bisphenol AF |
| DESEQ_ID | deseq_id | VARCHAR(50) | 否 | BTREE | 精确匹配 | DESEQ1001 |
| FromGroup | from_group | VARCHAR(255) | 否 | — | 不参与 | EU_EDCs,DEDuCT,TEDX |
| Evidence | evidence | VARCHAR(255) | 否 | — | 不参与 | (1)人类和小鼠证据 |
| GSE_ID | gse_id | VARCHAR(50) | 否 | — | 不参与；可跳外链 | GSE223464 |
| SRR_ID | srr_id | VARCHAR(50) | 否 | — | 不参与；可跳外链 | SRR23190066 |
| BioProject | bioproject_id | VARCHAR(50) | 否 | — | 不参与；可跳外链 | PRJNA926360 |
| AvgSpotLen | avg_spot_len | INT | 否 | — | 不参与 | 300 |
| cell_type | cell_type | VARCHAR(255) | 是 | — | 不参与 | uterus |
| *(空白列)* | library_layout | VARCHAR(20) | 否 | — | 不参与 | PAIRED |
| Organism | organism | VARCHAR(100) | 否 | — | 不参与 | Mus musculus |
| Platform | platform | VARCHAR(50) | 否 | — | 不参与 | ILLUMINA |
| treatment | treatment | VARCHAR(255) | 是 | — | 不参与 | BPAF |
| Group | experiment_group | VARCHAR(20) | 否 | — | 不参与 | treat |
| Chem_Name | chem_name | VARCHAR(255) | 是 | — | 不参与 | BPAF |
| 剂量 | dose | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 300 μg/kg |
| 时间 | exposure_time | VARCHAR(100) | 是 | — | 不参与 | 28d |
| 组织分类 | tissue_category | VARCHAR(100) | 否 | — | 不参与 | 癌症 |
| 组织细分 | tissue_subcategory | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 生殖系统癌症 |
| 生殖细分 | reproductive_subcategory | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 卵巢 |
| 组织/细胞系 | tissue_or_cell_line | VARCHAR(255) | 否 | — | 不参与 | 人乳腺癌细胞(MCF7) |
| 暴露毒物 | exposure_toxicant | VARCHAR(255) | 是 | — | 不参与；裸值0=缺失 | BPAF |
| 建库方法 | library_method | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | bulk_RNAseq |
| 详细建库方法 | library_method_detail | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | RNA-seq |
| 发表年 | publication_year | SMALLINT | 是 | — | 不参与；裸值0=缺失 | 2017 |
| 发表月 | publication_month | TINYINT | 是 | — | 不参与；裸值0=缺失 | 8 |
| 参考文献 | reference_title | TEXT | 是 | — | 不参与；裸值0=缺失 | Editor's Highlight: ... |
| DOI | doi | VARCHAR(255) | 是 | — | 不参与；可跳外链；裸值0=缺失 | 10.1093/toxsci/kfx101 |
| Class1 | class1_code | VARCHAR(20) | 否 | — | 不参与 | D02 |
| Class2 | class2_code | VARCHAR(20) | 否 | — | 不参与 | D02 |
| Class3 | class3_name | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 碳氢化合物 |
| Class4 | class4_name | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 碳氢化合物，环状 |
| Class5 | class5_name | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 芳烃 |
| Class6 | class6_name | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 苯衍生物 |
| Class7 | class7_name | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 酚类 |
| infer_Class | inferred_class | VARCHAR(100) | 是 | — | 不参与；裸值0=缺失 | 双酚类（旧） |

---

## 12. 前后端实现细节

### 12.1 后端项目结构

```text
backend/
├── app/
│   ├── __init__.py
│   ├── main.py                # FastAPI 应用入口、中间件、路由注册
│   ├── config.py              # 配置项（从环境变量读取）
│   ├── database.py            # SQLAlchemy engine & session
│   ├── models/
│   │   ├── __init__.py
│   │   ├── main_record.py     # MainRecord ORM 模型
│   │   └── record_asset.py    # RecordAsset ORM 模型
│   ├── schemas/
│   │   ├── __init__.py
│   │   ├── common.py          # 统一响应、分页结构
│   │   ├── search.py          # 搜索请求/响应
│   │   ├── analysis.py        # 详情页数据
│   │   └── stats.py           # 统计摘要
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── search.py          # /api/search, /api/search/export
│   │   ├── browse.py          # /api/browse, /api/browse/filters
│   │   ├── analysis.py        # /api/analysis/{key}, /api/analysis/{key}/tables/*
│   │   ├── assets.py          # /api/assets/{id}/download
│   │   ├── download.py        # /api/download/database
│   │   └── stats.py           # /api/stats/summary
│   └── services/
│       ├── __init__.py
│       ├── search_service.py   # 搜索与筛选业务逻辑
│       ├── analysis_service.py # 详情查询
│       ├── asset_service.py    # 资源文件管理
│       ├── txt_parser.py       # deg_table / plot_df 解析
│       └── export_service.py   # Excel 导出
├── scripts/
│   ├── import_excel.py         # Excel 导入脚本
│   └── scan_assets.py          # 资源文件扫描脚本
├── requirements.txt
├── Dockerfile
└── .env.example
```

### 12.2 环境变量

```bash
# 数据库
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=mousetoxdb
MYSQL_USER=mousetoxdb
MYSQL_PASSWORD=mousetoxdb_pass
MYSQL_ROOT_PASSWORD=root_pass

# 后端
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# 文件路径
DATA_DIR=/data
EXCEL_PATH=/data/source_excel/260307小鼠双端信息全.xlsx
ASSETS_DIR=/data/deseq_assets
STATISTICS_DIR=/data/statistics
```

### 12.3 CORS 配置

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],     # 生产环境应限定为实际域名
    allow_methods=["GET"],   # 只读系统，只需 GET
    allow_headers=["*"],
)
```

### 12.4 前端实现要求
- 所有列表页统一使用 Ant Design `Table` + `Pagination`
- 搜索框使用 Ant Design `Input.Search` + `Select`（分类下拉）
- 详情页字段区使用 `Descriptions` 组件
- 详情页 Tab 使用 `Tabs` 组件
- PDF 资源区使用卡片网格布局（Ant Design `Card` + `Row`/`Col`）
- DEG 表格使用 `Table` 组件，分页大小固定 30
- 所有长文本字段支持 tooltip 展开
- 空值字段显示 "—"

### 12.5 脚本说明

#### `scripts/import_excel.py`
- 读取 Excel 文件
- 按清洗规则处理数据
- 创建 `main_records` 表（如已存在则清空重建）
- 逐行插入数据
- 输出导入统计日志

#### `scripts/scan_assets.py`
- 扫描资源目录下所有 `DESEQ*/` 文件夹
- 按命名规则识别文件类别
- 创建/更新 `record_assets` 表记录
- 标记缺失或空文件夹的状态
- 输出扫描统计日志

### 12.6 错误处理
| 场景 | HTTP 状态码 | 前端展示 |
|------|-----------|---------|
| 搜索无结果 | 200 | 返回空列表 + 提示文字 |
| 详情不存在 | 404 | 页面显示"分析条目未找到" |
| 资源文件缺失 | 200（status=missing） | 卡片显示"当前文件未找到" |
| TXT 解析失败 | 200 + error 字段 | 显示"文件解析失败"，保留下载按钮 |
| 服务器内部错误 | 500 | 统一错误页 |

---

## 13. Docker 部署

### 13.1 项目目录结构

```text
mousetoxdb/
├── backend/
│   ├── app/
│   ├── scripts/
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   ├── public/
│   ├── package.json
│   ├── vite.config.ts
│   └── Dockerfile          # 多阶段构建：Node构建 → Nginx服务
├── nginx/
│   └── default.conf
├── data/
│   ├── source_excel/
│   │   └── 260307小鼠双端信息全.xlsx
│   ├── deseq_assets/
│   │   ├── DESEQ0005/
│   │   ├── DESEQ1001/
│   │   └── ...
│   └── statistics/
│       └── overall_summary.png
├── docker-compose.yml
├── .env
└── README.md
```

### 13.2 Nginx 配置（`nginx/default.conf`）

```nginx
server {
    listen 80;
    server_name localhost;
    client_max_body_size 50M;

    # 前端静态文件
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;   # SPA 路由回退
    }

    # 后端 API 反向代理
    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # FastAPI Swagger 文档
    location /docs {
        proxy_pass http://backend:8000/docs;
    }
    location /openapi.json {
        proxy_pass http://backend:8000/openapi.json;
    }

    # 静态资源文件（PDF/TXT）
    location /static/assets/ {
        alias /data/deseq_assets/;
        autoindex off;
    }

    # 统计图片
    location /static/statistics/ {
        alias /data/statistics/;
        autoindex off;
    }
}
```

### 13.3 Docker Compose（`docker-compose.yml`）

```yaml
version: "3.8"

services:
  mysql:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    restart: always
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      ASSETS_DIR: /data/deseq_assets
      EXCEL_PATH: /data/source_excel/260307小鼠双端信息全.xlsx
      STATISTICS_DIR: /data/statistics
    volumes:
      - ./data:/data:ro
    depends_on:
      mysql:
        condition: service_healthy

  nginx:
    build: ./frontend
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./data/deseq_assets:/data/deseq_assets:ro
      - ./data/statistics:/data/statistics:ro
    depends_on:
      - backend
      - mysql

volumes:
  mysql_data:
```

### 13.4 Frontend Dockerfile（多阶段构建）

```dockerfile
# 构建阶段
FROM node:18-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# 生产阶段
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 13.5 Backend Dockerfile

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 13.6 一键启动

```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env 设置密码等

# 2. 启动所有服务
docker compose up --build -d

# 3. 导入数据（首次启动后执行）
docker compose exec backend python scripts/import_excel.py
docker compose exec backend python scripts/scan_assets.py
```

启动后可访问：
| 服务 | 地址 |
|------|------|
| 前端首页 | http://localhost |
| API 文档 | http://localhost/docs |
| 数据库 | localhost:3306 |

---

## 14. 验收标准

核心目标：用户能够通过输入关键词准确搜索并展示对应信息。

### 14.1 功能验收

| # | 验收项 | 验收条件 |
|---|--------|---------|
| 1 | Excel 导入 | 986 条记录全部成功导入，无遗漏 |
| 2 | 全局搜索 | 输入 CAS 号、化学物名称、DESEQ_ID 等均能返回正确结果 |
| 3 | 分类搜索 | 选择特定分类后搜索范围正确缩小 |
| 4 | 搜索分页 | 结果超过 30 条时分页正确 |
| 5 | 浏览页 | 能展示所有 81 个分析条目，筛选功能正常 |
| 6 | 详情页-基本信息 | 点击 DESEQ_ID 进入详情页，展示所有字段 |
| 7 | 详情页-样本记录 | 展示关联的 SRR 样本列表 |
| 8 | 详情页-PDF 资源 | PDF 内嵌预览正确，缺失文件显示提示 |
| 9 | 详情页-DEG 表格 | deg_table.txt 正确解析为分页表格 |
| 10 | 外链跳转 | PubChem、GEO、SRA、BioProject、DOI 链接正确 |
| 11 | 整库下载 | 能下载完整 Excel 文件 |
| 12 | 结果导出 | 搜索结果能导出为 Excel |
| 13 | 统计页 | 静态统计图片正确展示 |
| 14 | Docker 启动 | `docker compose up --build` 后可正常访问所有功能 |

### 14.2 非功能验收
- 页面加载时间 < 3 秒
- 搜索响应时间 < 1 秒
- 移动端基本可用（响应式布局）

---

## 15. 当前状态与扩展方向

### 15.1 v1 已明确
- 网站公开访问，无登录/权限/后台
- 中文界面
- 全局搜索（7 字段 + 分类下拉）
- 浏览页（表格 + 侧边栏筛选）
- 详情页（5 个 Tab：基本信息、实验信息、样本记录、图表资源、DEG 表格）
- PDF 按 DESEQ_ID 文件夹组织，前端内嵌预览
- deg_table.txt 解析为分页表格
- plot_df.txt 仅下载
- 统计页使用静态图片
- Docker 一键启动（3 服务：mysql + backend + nginx）

### 15.2 v1 暂缓
- 中英文切换（i18n）
- 文献页详细交互与数据结构
- 动态统计图表
- 后台管理功能
- plot_df.txt 表格预览

### 15.3 后续可扩展方向
- 增加中英文切换
- 增加文献去重与文献详情页
- 增加高级筛选（多条件组合）
- 增加动态统计图表（ECharts / D3.js）
- 增加基因表达可视化（线框图 P4 第三层级）
- 增加毒物信息详情（线框图 P4 第四层级，参考 CTD）
- 增加资源自动补全与校验报告
- 增加批量外链校验
- 增加其他物种数据支持
