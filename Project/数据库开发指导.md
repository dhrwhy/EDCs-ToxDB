# MouseToxDB — 数据库开发指导

> **角色**：数据库开发
> **关联文档**：[后端开发指导](./后端开发指导.md) | [前端开发指导](./前端开发指导.md)
> **总文档**：[数据库项目.md](../数据库项目.md)

---

## 1. 概述

MouseToxDB 是一个小鼠毒理转录组科研数据库。数据库负责人的工作范围：

1. MySQL 实例配置与建库
2. 建表（主表 + 资源表）
3. 编写数据导入脚本（Excel → MySQL）
4. 编写资源扫描脚本（文件系统 → 资源表）
5. 验证数据完整性

> 本项目为**只读型数据库**，不使用 Alembic。表结构由脚本直接创建。

---

## 2. MySQL 实例配置

| 配置项 | 值 |
|--------|-----|
| 引擎 | MySQL 8.0 |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_general_ci（大小写不敏感） |
| 数据库名 | mousetoxdb |
| 用户名 | mousetoxdb |
| 密码 | mousetoxdb_pass（可在 .env 中修改） |

### Docker 启动命令参数

```
--character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
```

### 建库 SQL

```sql
CREATE DATABASE IF NOT EXISTS mousetoxdb
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_general_ci;
```

> Docker Compose 中 `MYSQL_DATABASE` 环境变量会自动建库，无需手动执行。

---

## 3. 表结构设计

### 3.1 主表：`main_records`

保存 Excel 中的所有原始字段（986 行 × 43 列），每一行对应一条 SRR 样本记录。额外添加 4 个系统内部字段。

```sql
CREATE TABLE main_records (
    -- ========== 系统字段 ==========
    record_pk          BIGINT AUTO_INCREMENT PRIMARY KEY,
    analysis_key       VARCHAR(128) NOT NULL              COMMENT '系统唯一分析键: {DESEQ_ID}__{ID}__{GSE_ID}__{BioProject}',
    created_at         DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at         DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    -- ========== Excel 源字段（43 列） ==========
    sort_id                  INT          NOT NULL                COMMENT 'Excel原始排序号',
    chemical_id              INT          NOT NULL                COMMENT '化学物质内部编号(ID列)',
    cas_id                   VARCHAR(50)  NOT NULL                COMMENT 'CAS编号',
    inchi_key                VARCHAR(100) NOT NULL                COMMENT 'InChIKey',
    chemical_name            VARCHAR(512) NOT NULL                COMMENT '化学物标准名称',
    alternative_names        TEXT         NULL                    COMMENT '化学物别名',
    pubchem_cid              VARCHAR(50)  NOT NULL                COMMENT 'PubChem化合物编号',
    pubchem_name             VARCHAR(255) NOT NULL                COMMENT 'PubChem名称',
    deseq_id                 VARCHAR(50)  NOT NULL                COMMENT 'DESeq分析编号(非唯一)',
    from_group               VARCHAR(255) NOT NULL                COMMENT '数据来源分组',
    evidence                 VARCHAR(255) NOT NULL                COMMENT '证据等级说明',
    gse_id                   VARCHAR(50)  NOT NULL                COMMENT 'GEO Series编号',
    srr_id                   VARCHAR(50)  NOT NULL                COMMENT 'SRA Run编号',
    bioproject_id            VARCHAR(50)  NOT NULL                COMMENT 'BioProject编号',
    avg_spot_len             INT          NOT NULL                COMMENT '平均reads/spot长度',
    cell_type                VARCHAR(255) NULL                    COMMENT '细胞类型/样本来源',
    library_layout           VARCHAR(20)  NOT NULL                COMMENT '测序布局(PAIRED/SINGLE)',
    organism                 VARCHAR(100) NOT NULL                COMMENT '物种',
    platform                 VARCHAR(50)  NOT NULL                COMMENT '测序平台',
    treatment                VARCHAR(255) NULL                    COMMENT '处理条件',
    experiment_group         VARCHAR(20)  NOT NULL                COMMENT '实验分组(con/treat)',
    chem_name                VARCHAR(255) NULL                    COMMENT '实验中化学名称/简称',
    dose                     VARCHAR(100) NULL                    COMMENT '暴露剂量',
    exposure_time            VARCHAR(100) NULL                    COMMENT '暴露时间',
    tissue_category          VARCHAR(100) NOT NULL                COMMENT '组织一级分类',
    tissue_subcategory       VARCHAR(100) NULL                    COMMENT '组织二级分类',
    reproductive_subcategory VARCHAR(100) NULL                    COMMENT '生殖系统细分',
    tissue_or_cell_line      VARCHAR(255) NOT NULL                COMMENT '具体组织或细胞系',
    exposure_toxicant        VARCHAR(255) NULL                    COMMENT '暴露毒物名称',
    library_method           VARCHAR(100) NULL                    COMMENT '建库方法',
    library_method_detail    VARCHAR(100) NULL                    COMMENT '详细建库方法',
    publication_year         SMALLINT     NULL                    COMMENT '发表年份',
    publication_month        TINYINT      NULL                    COMMENT '发表月份',
    reference_title          TEXT         NULL                    COMMENT '参考文献标题',
    doi                      VARCHAR(255) NULL                    COMMENT 'DOI编号',
    class1_code              VARCHAR(20)  NOT NULL                COMMENT 'MESH分类层级1',
    class2_code              VARCHAR(20)  NOT NULL                COMMENT 'MESH分类层级2',
    class3_name              VARCHAR(100) NULL                    COMMENT '分类层级3',
    class4_name              VARCHAR(100) NULL                    COMMENT '分类层级4',
    class5_name              VARCHAR(100) NULL                    COMMENT '分类层级5',
    class6_name              VARCHAR(100) NULL                    COMMENT '分类层级6',
    class7_name              VARCHAR(100) NULL                    COMMENT '分类层级7',
    inferred_class           VARCHAR(100) NULL                    COMMENT '推断分类',

    -- ========== 索引 ==========
    INDEX idx_analysis_key (analysis_key),  -- 非 UNIQUE：同一 analysis_key 对应多行 SRR 样本
    INDEX idx_chemical_id (chemical_id),
    INDEX idx_cas_id (cas_id),
    INDEX idx_inchi_key (inchi_key),
    INDEX idx_pubchem_cid (pubchem_cid),
    INDEX idx_deseq_id (deseq_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
  COMMENT='主表：Excel样本记录';
```

#### 关键设计说明

**为什么 `deseq_id` 不是唯一键？**

Excel 中有 3 个 `DESEQ_ID` 被复用于不同的分析条目：

| DESEQ_ID | analysis_key_1 | analysis_key_2 |
|----------|---------------|---------------|
| DESEQ0066 | DESEQ0066\_\_1515\_\_GSE182897\_\_PRJNA758256 | DESEQ0066\_\_296\_\_GSE162527\_\_PRJNA682163 |
| DESEQ0078 | DESEQ0078\_\_300\_\_GSE233282\_\_PRJNA975566 | DESEQ0078\_\_13\_\_GSE235586\_\_PRJNA986506 |
| DESEQ1011 | DESEQ1011\_\_585\_\_GSE136174\_\_PRJNA561500 | DESEQ1011\_\_567\_\_GSE136174\_\_PRJNA561500 |

因此使用 `analysis_key` 作为唯一标识：
```
analysis_key = "{DESEQ_ID}__{chemical_id}__{GSE_ID}__{BioProject}"
```

**`analysis_key` 不是主键**：同一个 `analysis_key` 对应多行记录（多个 SRR 样本），因此 `analysis_key` 有 UNIQUE INDEX 但**同一值在表中出现多次**。

> 修正：`analysis_key` 字段设为 NOT NULL，但**不设 UNIQUE 约束**。多个 SRR 样本共享同一个 `analysis_key`。UNIQUE INDEX 应去掉，改为普通 INDEX。

```sql
-- 正确的索引定义（替换上方 UNIQUE INDEX）
INDEX idx_analysis_key (analysis_key),
```

### 3.2 资源表：`record_assets`

保存与 `DESEQ_ID` 关联的 PDF/TXT 文件元信息。

```sql
CREATE TABLE record_assets (
    asset_id       BIGINT AUTO_INCREMENT PRIMARY KEY,
    deseq_id       VARCHAR(50)  NOT NULL                     COMMENT '资源所属DESEQ_ID',
    file_name      VARCHAR(255) NOT NULL                     COMMENT '原始文件名',
    file_path      TEXT         NOT NULL                     COMMENT '服务器相对路径',
    file_ext       VARCHAR(10)  NOT NULL                     COMMENT '文件扩展名(pdf/txt)',
    asset_category VARCHAR(50)  NOT NULL                     COMMENT '资源分类',
    display_name   VARCHAR(100) NOT NULL                     COMMENT '前端显示名称',
    sort_order     INT          NOT NULL DEFAULT 0           COMMENT '展示排序',
    parse_mode     VARCHAR(20)  NOT NULL DEFAULT 'none'      COMMENT '解析模式: none / table_preview',
    status         VARCHAR(20)  NOT NULL DEFAULT 'available' COMMENT '状态: available / missing / pending',
    remark         TEXT         NULL                         COMMENT '备注',
    created_at     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at     DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_deseq_id (deseq_id),
    INDEX idx_asset_category (asset_category),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci
  COMMENT='资源表：DESEQ_ID关联的PDF/TXT文件';
```

#### status 字段取值

| 值 | 含义 | 触发条件 |
|------|------|---------|
| `available` | 文件存在且可访问 | 扫描时文件存在 |
| `missing` | 文件预期存在但实际缺失 | 扫描时文件不存在 |
| `pending` | 资源待补充 | 文件夹为空或不存在 |

### 3.3 两表关系

```
main_records.deseq_id  ──(多对多)──  record_assets.deseq_id
```

- 没有外键约束（因为资源可能先于主表数据存在，如 DESEQ0005）
- 通过 `deseq_id` 字段进行应用层关联
- 后端查询时 JOIN 两表

---

## 4. 数据源与导入

### 4.1 Excel 数据源

| 属性 | 值 |
|------|-----|
| 文件 | `260307小鼠双端信息全.xlsx` |
| 工作表 | `Sheet1` |
| 表头行 | 第 3 行 |
| 数据起始行 | 第 4 行 |
| 数据总行数 | 986 |
| 源字段数 | 43 |

### 4.2 Excel 列名 → 数据库字段名映射

| # | Excel 列名 | 数据库字段名 | 类型 | 可空 |
|---|-----------|-----------|------|------|
| 1 | Sort_ID | sort_id | INT | 否 |
| 2 | ID | chemical_id | INT | 否 |
| 3 | CAS_ID | cas_id | VARCHAR(50) | 否 |
| 4 | InChIKey | inchi_key | VARCHAR(100) | 否 |
| 5 | Chemical_Name | chemical_name | VARCHAR(512) | 否 |
| 6 | Alternative_Names | alternative_names | TEXT | 是 |
| 7 | PubChem_CID | pubchem_cid | VARCHAR(50) | 否 |
| 8 | PubChem_Name | pubchem_name | VARCHAR(255) | 否 |
| 9 | DESEQ_ID | deseq_id | VARCHAR(50) | 否 |
| 10 | FromGroup | from_group | VARCHAR(255) | 否 |
| 11 | Evidence | evidence | VARCHAR(255) | 否 |
| 12 | GSE_ID | gse_id | VARCHAR(50) | 否 |
| 13 | SRR_ID | srr_id | VARCHAR(50) | 否 |
| 14 | BioProject | bioproject_id | VARCHAR(50) | 否 |
| 15 | AvgSpotLen | avg_spot_len | INT | 否 |
| 16 | cell_type | cell_type | VARCHAR(255) | 是 |
| 17 | *(第17列空白)* | library_layout | VARCHAR(20) | 否 |
| 18 | Organism | organism | VARCHAR(100) | 否 |
| 19 | Platform | platform | VARCHAR(50) | 否 |
| 20 | treatment | treatment | VARCHAR(255) | 是 |
| 21 | Group | experiment_group | VARCHAR(20) | 否 |
| 22 | Chem_Name | chem_name | VARCHAR(255) | 是 |
| 23 | 剂量 | dose | VARCHAR(100) | 是 |
| 24 | 时间 | exposure_time | VARCHAR(100) | 是 |
| 25 | 组织分类 | tissue_category | VARCHAR(100) | 否 |
| 26 | 组织细分 | tissue_subcategory | VARCHAR(100) | 是 |
| 27 | 生殖细分 | reproductive_subcategory | VARCHAR(100) | 是 |
| 28 | 组织/细胞系 | tissue_or_cell_line | VARCHAR(255) | 否 |
| 29 | 暴露毒物 | exposure_toxicant | VARCHAR(255) | 是 |
| 30 | 建库方法 | library_method | VARCHAR(100) | 是 |
| 31 | 详细建库方法 | library_method_detail | VARCHAR(100) | 是 |
| 32 | 发表年（1900-2030） | publication_year | SMALLINT | 是 |
| 33 | 发表月（1-12） | publication_month | TINYINT | 是 |
| 34 | 参考文献 | reference_title | TEXT | 是 |
| 35 | DOI | doi | VARCHAR(255) | 是 |
| 36 | Class1 | class1_code | VARCHAR(20) | 否 |
| 37 | Class2 | class2_code | VARCHAR(20) | 否 |
| 38 | Class3 | class3_name | VARCHAR(100) | 是 |
| 39 | Class4 | class4_name | VARCHAR(100) | 是 |
| 40 | Class5 | class5_name | VARCHAR(100) | 是 |
| 41 | Class6 | class6_name | VARCHAR(100) | 是 |
| 42 | Class7 | class7_name | VARCHAR(100) | 是 |
| 43 | infer_Class | inferred_class | VARCHAR(100) | 是 |

### 4.3 数据清洗规则

#### 规则 1：去除空白字符
- 所有字符串值去除首尾空格
- 去除 `\xa0`（不间断空格）等不可见字符

#### 规则 2：空字符串 → NULL
- 空字符串统一转为 `NULL`

#### 规则 3：裸值 "0" → NULL
以下字段中，若值为 `"0"` 或数值 `0`，按 `NULL` 处理：

```
dose, tissue_subcategory, reproductive_subcategory, exposure_toxicant,
library_method, library_method_detail, publication_year, publication_month,
reference_title, doi, class3_name, class4_name, class5_name,
class6_name, class7_name, inferred_class
```

> 例外：`"0 ppm"`、`"0.1 mg/kg"` 带单位的字符串不视为缺失。判断条件：`str(value).strip() == "0"`

#### 规则 4：数值字段转换
以下字段转为整数：`sort_id`, `chemical_id`, `avg_spot_len`, `publication_year`, `publication_month`

#### 规则 5：生成 analysis_key
```python
analysis_key = f"{deseq_id}__{chemical_id}__{gse_id}__{bioproject_id}"
```

### 4.4 导入脚本：`scripts/import_excel.py`

**功能**：
1. 连接 MySQL
2. 创建 `main_records` 表（如已存在则 DROP 重建）
3. 读取 Excel（Sheet1，第 3 行表头，第 4 行起数据）
4. 按清洗规则处理每行
5. 生成 `analysis_key`
6. 批量 INSERT
7. 输出导入统计日志

**执行方式**：
```bash
docker compose exec backend python scripts/import_excel.py
```

**预期输出**：
```
[INFO] 已读取 986 行数据
[INFO] 清洗完成，生成 986 条记录
[INFO] 唯一 analysis_key 数: 81
[INFO] 导入完成: 986 行已插入 main_records
```

### 4.5 资源扫描脚本：`scripts/scan_assets.py`

**功能**：
1. 连接 MySQL
2. 创建 `record_assets` 表（如已存在则 DROP 重建）
3. 扫描 `/data/deseq_assets/` 下所有 `DESEQ*/` 文件夹
4. 按文件名关键词匹配资源类别
5. 检查文件是否实际存在，设置 `status`
6. 批量 INSERT
7. 输出扫描统计日志

**文件名 → 类别映射规则**：

| 文件名关键词 | asset_category | display_name | sort_order | parse_mode |
|------------|---------------|-------------|-----------|------------|
| `PCA_plot` | pca_plot | PCA 图 | 10 | none |
| `Volcano_plot` | volcano_plot | 火山图 | 20 | none |
| `Heatmap` | heatmap | 热图 | 30 | none |
| `KEGG_Up_plot` | kegg_up_plot | KEGG 上调通路 | 40 | none |
| `KEGG_Down_plot` | kegg_down_plot | KEGG 下调通路 | 50 | none |
| `Reactome_Up_plot` | reactome_up_plot | Reactome 上调通路 | 60 | none |
| `Reactome_Down_plot` | reactome_down_plot | Reactome 下调通路 | 70 | none |
| `Hallmark_Up_plot` | hallmark_up_plot | Hallmark 上调通路 | 80 | none |
| `DO_Up_plot` | do_up_plot | DO 上调通路 | 90 | none |
| `deg_table.txt` | deg_table | DEG 差异基因表 | 100 | table_preview |
| `plot_df.txt` | plot_df | 绘图数据 | 110 | none |

**执行方式**：
```bash
docker compose exec backend python scripts/scan_assets.py
```

---

## 5. 数据完整性验证

导入完成后，执行以下 SQL 验证：

```sql
-- 1. 总行数
SELECT COUNT(*) AS total_rows FROM main_records;
-- 预期: 986

-- 2. 唯一 analysis_key 数
SELECT COUNT(DISTINCT analysis_key) AS unique_keys FROM main_records;
-- 预期: 81

-- 3. 唯一化学物数
SELECT COUNT(DISTINCT chemical_id) AS unique_chemicals FROM main_records;
-- 预期: 36

-- 4. 唯一 DESEQ_ID 数
SELECT COUNT(DISTINCT deseq_id) AS unique_deseq FROM main_records;
-- 预期: 78

-- 5. 唯一 SRR_ID 数
SELECT COUNT(DISTINCT srr_id) AS unique_srr FROM main_records;
-- 预期: 770

-- 6. 复用的 DESEQ_ID（应返回 3 行）
SELECT deseq_id, COUNT(DISTINCT analysis_key) AS key_count
FROM main_records
GROUP BY deseq_id
HAVING key_count > 1;

-- 7. 资源表记录数
SELECT COUNT(*) AS total_assets FROM record_assets;

-- 8. 资源状态分布
SELECT status, COUNT(*) FROM record_assets GROUP BY status;
```

---

## 6. 与后端的接口约定

> 本节说明数据库向后端提供的数据接口，确保两个组件能正确对接。

### 6.1 连接方式

后端通过 SQLAlchemy 2.0 连接 MySQL：

```python
# 连接字符串格式
DATABASE_URL = "mysql+pymysql://{user}:{password}@{host}:{port}/{database}?charset=utf8mb4"

# Docker 环境下的值
DATABASE_URL = "mysql+pymysql://mousetoxdb:mousetoxdb_pass@mysql:3306/mousetoxdb?charset=utf8mb4"
```

### 6.2 后端需要的典型查询

**搜索（按 analysis_key 去重，返回分析条目级结果）**：
```sql
SELECT
    analysis_key, deseq_id, chemical_id, sort_id, cas_id,
    chemical_name, pubchem_cid, pubchem_name, gse_id,
    bioproject_id, organism, tissue_category, library_method,
    platform, publication_year,
    COUNT(*) AS sample_count
FROM main_records
WHERE cas_id = :keyword          -- 精确匹配
   OR inchi_key = :keyword       -- 精确匹配
   OR pubchem_cid = :keyword     -- 精确匹配
   OR deseq_id = :keyword        -- 精确匹配
   OR chemical_name LIKE :like   -- 模糊匹配
   OR alternative_names LIKE :like
   OR pubchem_name LIKE :like
GROUP BY analysis_key
ORDER BY chemical_id ASC, deseq_id ASC
LIMIT :page_size OFFSET :offset;
```

**详情（某 analysis_key 的汇总信息 + 样本列表）**：
```sql
-- 汇总（取第一条的非样本字段）
SELECT * FROM main_records WHERE analysis_key = :key LIMIT 1;

-- 样本列表
SELECT srr_id, avg_spot_len, cell_type, library_layout,
       treatment, experiment_group, chem_name, dose, exposure_time
FROM main_records
WHERE analysis_key = :key;
```

**资源查询**：
```sql
SELECT * FROM record_assets
WHERE deseq_id = :deseq_id
ORDER BY sort_order ASC;
```

**统计摘要**：
```sql
SELECT
    COUNT(*)                        AS record_rows,
    COUNT(DISTINCT analysis_key)    AS analysis_groups,
    COUNT(DISTINCT chemical_id)     AS unique_chemicals,
    COUNT(DISTINCT deseq_id)        AS unique_deseq_id,
    COUNT(DISTINCT srr_id)          AS unique_srr_id,
    COUNT(DISTINCT gse_id)          AS unique_gse_id,
    COUNT(DISTINCT bioproject_id)   AS unique_bioproject_id
FROM main_records;
```

**浏览筛选选项**：
```sql
SELECT DISTINCT tissue_category FROM main_records WHERE tissue_category IS NOT NULL ORDER BY tissue_category;
SELECT DISTINCT library_method FROM main_records WHERE library_method IS NOT NULL ORDER BY library_method;
SELECT MIN(publication_year) AS min_year, MAX(publication_year) AS max_year FROM main_records WHERE publication_year IS NOT NULL;
```

### 6.3 后端 ORM 映射

后端使用 SQLAlchemy 2.0 ORM。数据库字段名 = ORM 属性名（snake_case）。两个 Model 类：

- `MainRecord` → `main_records` 表
- `RecordAsset` → `record_assets` 表

---

## 7. Docker 配置

### 7.1 MySQL 服务（docker-compose.yml 中的部分）

```yaml
mysql:
  image: mysql:8.0
  restart: always
  environment:
    MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    MYSQL_DATABASE: ${MYSQL_DATABASE}
    MYSQL_USER: ${MYSQL_USER}
    MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  ports:
    - "3306:3306"
  volumes:
    - mysql_data:/var/lib/mysql
  command: --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci
  healthcheck:
    test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
    interval: 10s
    timeout: 5s
    retries: 5
```

### 7.2 环境变量（`.env`）

```bash
MYSQL_ROOT_PASSWORD=root_pass
MYSQL_DATABASE=mousetoxdb
MYSQL_USER=mousetoxdb
MYSQL_PASSWORD=mousetoxdb_pass
```

### 7.3 数据持久化

- MySQL 数据存储在 Docker Volume `mysql_data` 中
- `docker compose down` 不会删除数据
- `docker compose down -v` 会删除数据（需重新导入）

---

## 8. 验收清单

| # | 验收项 | 验收方法 |
|---|--------|---------|
| 1 | 数据库实例正常运行 | `docker compose exec mysql mysql -u mousetoxdb -p mousetoxdb -e "SELECT 1"` |
| 2 | 表结构正确创建 | `SHOW CREATE TABLE main_records;` `SHOW CREATE TABLE record_assets;` |
| 3 | 986 条记录全部导入 | `SELECT COUNT(*) FROM main_records;` → 986 |
| 4 | 81 个唯一 analysis_key | `SELECT COUNT(DISTINCT analysis_key) FROM main_records;` → 81 |
| 5 | 索引正确创建 | `SHOW INDEX FROM main_records;` |
| 6 | 资源记录已扫描 | `SELECT COUNT(*) FROM record_assets;` |
| 7 | 清洗规则生效 | `SELECT COUNT(*) FROM main_records WHERE dose = '0';` → 0 |
| 8 | 字符集正确 | `SHOW VARIABLES LIKE 'character_set_database';` → utf8mb4 |
